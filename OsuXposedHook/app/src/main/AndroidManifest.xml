<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="OSU Hooks"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.LaunchBackground"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.LaunchBackground">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="xposedmodule"
            android:value="true" />

        <meta-data
            android:name="xposedminversion"
            android:value="82" />

        <meta-data
            android:name="xposeddescription"
            android:value="Xposed module for osu!lazer - big hit windows, no-fail, auto-pilot, unlock all beatmaps, no ads" />

        <meta-data
            android:name="xposedscope"
            android:value="sh.ppy.osu"
            tools:replace="android:value" />

    </application>

</manifest>